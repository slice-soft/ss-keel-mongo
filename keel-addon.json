{
  "name": "ss-keel-mongo",
  "version": "0.1.0",
  "description": "MongoDB via official mongo-driver for document-first workflows",
  "repo": "github.com/slice-soft/ss-keel-mongo",
  "steps": [
    {
      "type": "go_get",
      "package": "github.com/slice-soft/ss-keel-mongo"
    },
    {
      "type": "property",
      "key": "mongo.uri",
      "example": "${MONGO_URI:mongodb://localhost:27017}",
      "description": "MongoDB connection URI."
    },
    {
      "type": "property",
      "key": "mongo.database",
      "example": "${MONGO_DATABASE:app}",
      "description": "MongoDB database name."
    },
    {
      "type": "env",
      "key": "MONGO_URI",
      "example": "mongodb://localhost:27017",
      "description": "MongoDB connection URI. Atlas example: mongodb+srv://user:pass@cluster.mongodb.net"
    },
    {
      "type": "env",
      "key": "MONGO_DATABASE",
      "example": "app",
      "description": "MongoDB database name"
    },
    {
      "type": "create_provider_file",
      "filename": "cmd/setup_mongo.go",
      "guard": "func setupMongo(",
      "content": "package main\n\nimport (\n\t\"github.com/slice-soft/ss-keel-core/config\"\n\t\"github.com/slice-soft/ss-keel-core/core\"\n\t\"github.com/slice-soft/ss-keel-core/logger\"\n\t\"github.com/slice-soft/ss-keel-mongo/mongo\"\n)\n\n// setupMongo initialises the MongoDB client and registers a health checker.\nfunc setupMongo(app *core.App, log *logger.Logger) *mongo.Client {\n\tmongoConfig := config.MustLoadConfig[mongo.Config]()\n\tmongoConfig.Logger = log\n\n\tmongoClient, err := mongo.New(mongoConfig)\n\tif err != nil {\n\t\tlog.Error(\"failed to initialize MongoDB: %v\", err)\n\t}\n\tapp.RegisterHealthChecker(mongo.NewHealthChecker(mongoClient))\n\treturn mongoClient\n}\n"
    },
    {
      "type": "main_code",
      "anchor": "before_modules",
      "guard": "setupMongo(",
      "code": "mongoClient := setupMongo(app, appLogger)\ndefer mongoClient.Close()"
    }
  ]
}
